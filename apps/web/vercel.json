{
  "framework": "vite",
  "installCommand": "cd ../.. && npm install && (test -d node_modules/@rollup/rollup-linux-x64-gnu || npm install --no-save @rollup/rollup-linux-x64-gnu@$(node -p \"require('./node_modules/rollup/package.json').version\"))",
  "buildCommand": "cd ../.. && npx turbo run build --filter=web",
  "outputDirectory": "dist"
}
